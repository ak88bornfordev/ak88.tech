# Скопируйте этот файл в .env (в той же папке) и впишите свой токен.
# Файл .env НИКОГДА не должен попадать в git / публичный репозиторий —
# он уже добавлен в .gitignore.

TELEGRAM_BOT_TOKEN=впишите_сюда_токен_бота_от_BotFather
